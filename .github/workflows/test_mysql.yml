on:
  push:
  workflow_dispatch:

jobs:
  test-mysql:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Check MySQL installation
        run: Get-ChildItem "C:\Program Files\MySQL" -Recurse -Filter "mysqld.exe"
        shell: powershell

